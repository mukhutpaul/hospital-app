import { prisma } from "@/lib/prisma";
export default async function HospitalisationDashboard(){const [enCours,litsLibres,litsOccupes,entrees,sorties,chambres,soins,transferts]=await Promise.all([prisma.hospitalisation.count({where:{statut:"EN_COURS"}}),prisma.lit.count({where:{statut:"LIBRE"}}),prisma.lit.count({where:{statut:"OCCUPE"}}),prisma.hospitalisation.count({where:{dateEntree:{gte:new Date(new Date().setHours(0,0,0,0))}}}),prisma.sortie.count({where:{dateSortie:{gte:new Date(new Date().setHours(0,0,0,0))}}}),prisma.chambre.count({where:{actif:true}}),prisma.soin.count(),prisma.transfert.count()]);const cards=[['Hospitalisés',enCours],['Lits libres',litsLibres],['Lits occupés',litsOccupes],['Entrées aujourd’hui',entrees],['Sorties aujourd’hui',sorties],['Chambres actives',chambres],['Soins',soins],['Transferts',transferts]];return <main className="space-y-6"><div><h1 className="text-3xl font-bold">Hospitalisation</h1><p className="opacity-60">Gestion des hospitalisations, chambres, lits, soins, transferts et sorties.</p></div><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{cards.map(([label,value])=><div key={String(label)} className="rounded-2xl border border-base-300 bg-base-100 p-5 shadow-sm"><p className="text-sm opacity-60">{label}</p><p className="mt-2 text-3xl font-bold">{value}</p></div>)}</div><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3"><a className="btn btn-primary" href="/hospitalisation/hospitalisations">Hospitalisations</a><a className="btn btn-outline" href="/hospitalisation/chambres">Chambres</a><a className="btn btn-outline" href="/hospitalisation/lits">Lits</a><a className="btn btn-outline" href="/hospitalisation/soins">Soins</a><a className="btn btn-outline" href="/hospitalisation/transferts">Transferts</a><a className="btn btn-outline" href="/hospitalisation/sorties">Sorties</a></div></main>}
