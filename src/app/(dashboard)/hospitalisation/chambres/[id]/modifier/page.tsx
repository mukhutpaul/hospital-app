import {prisma} from "@/lib/prisma";import {getChambreById} from "@/app/actions/chambres";import {notFound} from "next/navigation";import ChambreForm from "@/components/hospitalisation/ChambreForm";export default async function Page({params}:{params:Promise<{id:string}>}){const r=await getChambreById(Number((await params).id));if(!r.success||!r.data)return notFound();const services=await prisma.service.findMany({where:{actif:true},orderBy:{nom:"asc"}});return <main className="mx-auto max-w-4xl p-6"><ChambreForm initial={r.data} services={services}/></main>}
