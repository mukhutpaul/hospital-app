import {getChambreById} from "@/app/actions/chambres";import {notFound} from "next/navigation";import Link from "next/link";export default async function Page({params}:{params:Promise<{id:string}>}){const r=await getChambreById(Number((await params).id));if(!r.success||!r.data)return notFound();const c=r.data as any;return <main className="space-y-5 p-6"><div className="flex justify-between"><h1 className="text-2xl font-bold">Chambre {c.numero}</h1><Link className="btn btn-outline" href={`/hospitalisation/chambres/${c.id}/modifier`}>Modifier</Link></div><div className="rounded-2xl border p-5">Service : {c.service?.nom||"—"} · Type : {c.type||"—"} · Étage : {c.etage||"—"}</div><div className="overflow-x-auto rounded-2xl border"><table className="table"><thead><tr><th>Lit</th><th>Statut</th></tr></thead><tbody>{c.lits.map((l:any)=><tr key={l.id}><td>{l.numero}</td><td><span className="badge">{l.statut}</span></td></tr>)}</tbody></table></div></main>}
