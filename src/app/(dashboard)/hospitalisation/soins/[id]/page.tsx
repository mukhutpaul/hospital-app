import {getSoinById} from "@/app/actions/soins";import {notFound} from "next/navigation";export default async function Page({params}:{params:Promise<{id:string}>}){const r=await getSoinById(Number((await params).id));if(!r.success||!r.data)return notFound();const s=r.data as any;return <main className="p-6"><div className="rounded-2xl border p-6 space-y-3"><h1 className="text-2xl font-bold">Soin #{s.id}</h1><p><b>Type :</b> {s.type}</p><p><b>Patient :</b> {[s.hospitalisation?.patient?.nom,s.hospitalisation?.patient?.prenom].filter(Boolean).join(" ")}</p><p><b>Date :</b> {new Date(s.dateSoin).toLocaleString("fr-FR")}</p><p><b>Description :</b> {s.description||"—"}</p><p><b>Observation :</b> {s.observation||"—"}</p></div></main>}
