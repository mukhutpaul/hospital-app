import {getSoinById} from "@/app/actions/soins";import {notFound} from "next/navigation";import SoinForm from "@/components/hospitalisation/SoinForm";export default async function Page({params}:{params:Promise<{id:string}>}){const r=await getSoinById(Number((await params).id));if(!r.success||!r.data)return notFound();return <main className="mx-auto max-w-3xl p-6"><SoinForm initial={r.data}/></main>}
