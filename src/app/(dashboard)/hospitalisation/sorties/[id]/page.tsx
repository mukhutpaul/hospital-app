import {getSortieById} from "@/app/actions/sorties";import {notFound} from "next/navigation";export default async function Page({params}:{params:Promise<{id:string}>}){const r=await getSortieById(Number((await params).id));if(!r.success||!r.data)return notFound();const s=r.data as any;return <main className="p-6"><div className="rounded-2xl border p-6 space-y-3"><h1 className="text-2xl font-bold">Sortie #{s.id}</h1><p><b>Patient :</b> {[s.patient?.nom,s.patient?.postNom,s.patient?.prenom].filter(Boolean).join(" ")}</p><p><b>Type :</b> {s.type}</p><p><b>Date :</b> {new Date(s.dateSortie).toLocaleString("fr-FR")}</p><p><b>Diagnostic final :</b> {s.diagnosticFinal||"—"}</p><p><b>Recommandation :</b> {s.recommandation||"—"}</p><p><b>Traitement :</b> {s.traitement||"—"}</p></div></main>}
