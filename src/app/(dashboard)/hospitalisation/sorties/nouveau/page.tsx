import {prisma} from "@/lib/prisma";import SortieForm from "@/components/hospitalisation/SortieForm";export default async function Page({searchParams}:{searchParams:Promise<{hospitalisationId?:string}>}){const q=await searchParams;const hospitalisations=await prisma.hospitalisation.findMany({where:{statut:"EN_COURS"},include:{patient:true},orderBy:{dateEntree:"desc"}});return <main className="mx-auto max-w-3xl p-6"><SortieForm hospitalisations={hospitalisations} initial={q.hospitalisationId?{hospitalisationId:Number(q.hospitalisationId)}:undefined}/></main>}
