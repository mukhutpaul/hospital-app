import Link from "next/link";import {getHospitalisationById} from "@/app/actions/hospitalisations";import {notFound} from "next/navigation";
export default async function Page({params}:{params:Promise<{id:string}>}){const {id}=await params;const r=await getHospitalisationById(Number(id));if(!r.success||!r.data)return notFound();const h=r.data as any;return <main className="space-y-5 p-6"><div className="flex justify-between"><div><h1 className="text-2xl font-bold">{h.numero}</h1><p className="opacity-60">{[h.patient?.nom,h.patient?.postNom,h.patient?.prenom].filter(Boolean).join(" ")}</p></div><Link className="btn btn-outline" href={`/hospitalisation/hospitalisations/${h.id}/modifier`}>Modifier</Link></div><div className="grid gap-4 md:grid-cols-3"><div className="rounded-2xl border p-5"><b>Service</b><p>{h.service?.nom??"—"}</p></div><div className="rounded-2xl border p-5"><b>Médecin</b><p>Dr {[h.medecin?.nom,h.medecin?.postNom,h.medecin?.prenom].filter(Boolean).join(" ")||"—"}</p></div><div className="rounded-2xl border p-5"><b>Chambre / Lit</b><p>{h.lit?`${h.lit.chambre?.numero} / ${h.lit.numero}`:"—"}</p></div></div><div className="rounded-2xl border p-5"><b>Motif</b><p className="mt-2">{h.motif||"—"}</p><b className="mt-4 block">Diagnostic</b><p className="mt-2">{h.diagnostic||"—"}</p></div><div className="grid gap-3 md:grid-cols-3"><Link className="btn btn-outline" href={`/hospitalisation/soins/nouveau?hospitalisationId=${h.id}`}>Ajouter un soin</Link><Link className="btn btn-outline" href={`/hospitalisation/transferts/nouveau?hospitalisationId=${h.id}`}>Transférer</Link><Link className="btn btn-outline" href={`/hospitalisation/sorties/nouveau?hospitalisationId=${h.id}`}>Enregistrer la sortie</Link></div><section><h2 className="mb-3 text-xl font-bold">Soins</h2><div className="overflow-x-auto rounded-xl border"><table className="table"><thead><tr><th>Date</th><th>Type</th><th>Description</th><th>Observation</th></tr></thead><tbody>{h.soins.map((s:any)=><tr key={s.id}><td>{new Date(s.dateSoin).toLocaleString("fr-FR")}</td><td>{s.type}</td><td>{s.description||"—"}</td><td>{s.observation||"—"}</td></tr>)}</tbody></table></div></section></main>}
