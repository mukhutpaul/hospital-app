import { prisma } from "@/lib/prisma";
export default async function Page({params}:{params:Promise<{id:string}>}){ const {id}=await params; const p=await prisma.proforma.findUnique({where:{id:Number(id)},include:{patient:true,consultation:{include:{medecin:true,service:true}},lignes:true}}); if(!p)return <div>Introuvable</div>; return <div className="mx-auto max-w-3xl p-8 print:p-0"><div className="text-center"><h1 className="text-2xl font-bold">NOM DE L'HÔPITAL</h1><p>Adresse — Téléphone</p><h2 className="mt-6 text-2xl font-bold">PROFORMA</h2><p>{p.numero} — {new Date(p.dateEmission).toLocaleDateString("fr-FR")}</p></div><div className="my-6 border-y py-4"><b>Patient :</b> {p.patient.nom} {p.patient.postNom||""} {p.patient.prenom||""} — {p.patient.numeroDossier}<br/><b>Consultation :</b> {p.consultation?`CONS-${p.consultation.idConsultation}`:"—"} — {p.consultation?.service?.nom||"—"} — Dr {p.consultation?.medecin?.nom||"—"}</div><table className="w-full"><thead><tr className="border-b text-left"><th>Origine</th><th>Désignation</th><th>Qté</th><th>Total</th></tr></thead><tbody>{p.lignes.map((l:any)=><tr className="border-b" key={l.id}><td>{l.typeOrigine}</td><td>{l.designation}</td><td>{l.quantite}</td><td>{l.montant.toFixed(2)} {p.devise}</td></tr>)}</tbody></table><div className="mt-6 ml-auto max-w-xs space-y-1 text-right"><div>Brut : {p.montantBrut.toFixed(2)} {p.devise}</div><div>Réduction : {p.reduction.toFixed(2)} {p.devise}</div><div className="text-xl font-bold">Net à payer : {p.montantTotal.toFixed(2)} {p.devise}</div></div><p className="mt-10 text-center text-sm">DOCUMENT PROFORMA — NON CONSTITUTIF D'UN REÇU</p><script dangerouslySetInnerHTML={{__html:'window.addEventListener("load",()=>window.print())'}} /></div> }
