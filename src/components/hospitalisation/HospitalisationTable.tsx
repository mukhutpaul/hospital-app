"use client";
import Link from "next/link";
import ActionButton from "./ActionButton";
export default function HospitalisationTable({items}:{items:any[]}){return <div className="overflow-x-auto rounded-2xl border border-base-300 bg-base-100"><table className="table"><thead><tr><th>N°</th><th>Patient</th><th>Service</th><th>Chambre / Lit</th><th>Entrée</th><th>Statut</th><th className="text-right">Actions</th></tr></thead><tbody>{items.map(h=><tr key={h.id}><td className="font-semibold">{h.numero}</td><td>{[h.patient?.nom,h.patient?.postNom,h.patient?.prenom].filter(Boolean).join(" ")}</td><td>{h.service?.nom??"—"}</td><td>{h.lit?`${h.lit.chambre?.numero??"—"} / ${h.lit.numero}`:"—"}</td><td>{new Date(h.dateEntree).toLocaleDateString("fr-FR")}</td><td><span className={`badge ${h.statut==="EN_COURS"?"badge-success":"badge-ghost"}`}>{h.statut}</span></td><td><div className="flex justify-end gap-1"><Link className="btn btn-ghost btn-sm" href={`/hospitalisation/hospitalisations/${h.id}`}>Voir</Link><Link className="btn btn-outline btn-sm" href={`/hospitalisation/hospitalisations/${h.id}/modifier`}>Modifier</Link>{h.statut==="EN_COURS"&&<ActionButton entity="hospitalisation" id={h.id} action="terminer" label="Terminer" className="btn btn-warning btn-sm"/>} {h.statut!=="EN_COURS"&&<ActionButton entity="hospitalisation" id={h.id}/>}</div></td></tr>)}</tbody></table></div>}
